/** @type {const} */
const themeColors = {
  primary: { light: '#0a7ea4', dark: '#3b82f6' }, // Brighter blue for dark mode
  background: { light: '#ffffff', dark: '#0f1419' }, // Darker, richer background
  surface: { light: '#f5f5f5', dark: '#1a1f26' }, // Elevated surface color
  foreground: { light: '#11181C', dark: '#e5e7eb' }, // Brighter text for dark mode
  muted: { light: '#687076', dark: '#9ca3af' }, // Lighter muted text
  border: { light: '#E5E7EB', dark: '#374151' }, // Subtle borders
  success: { light: '#22C55E', dark: '#10b981' }, // Vibrant green
  warning: { light: '#F59E0B', dark: '#f59e0b' }, // Consistent warning
  error: { light: '#EF4444', dark: '#ef4444' }, // Consistent error
};

module.exports = { themeColors };
